<!-- SignInContentDialog.xaml -->
<ContentDialog
    x:Class="WinUi.Pages.AddRedisServerDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUi"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Add Redis Server"
    PrimaryButtonText="{x:Bind DefaultPrimaryButtonText, Mode=OneTime}"
    CloseButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    IsPrimaryButtonEnabled="{x:Bind IsFormValid}">

    <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="0" Padding="0">

        <TextBox x:Name="serverTxt" Header="Server"/>
        <NumberBox x:Name="portTxt" Header="Port" SpinButtonPlacementMode="Hidden"/>
        
        <TextBox x:Name="usernameTxt" Header="Username"/>
        <PasswordBox x:Name="passwordTxt" Header="Password" IsPasswordRevealButtonEnabled="True"/>

        <TextBlock Foreground="Red" x:Name="errorTextBlock"/>
    </StackPanel>
</ContentDialog>